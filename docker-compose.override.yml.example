version: '3.8'

services:
  app:
    volumes:
      # Uncomment and modify these lines to use custom paths for data storage
      # - /path/to/your/uploads:/data/uploads
      # - /path/to/your/database:/data/instance
    environment:
      # Admin user creation (uncomment and set these to create an admin user on first run)
      # - ADMIN_USERNAME=admin
      # - ADMIN_EMAIL=admin@example.com
      # - ADMIN_PASSWORD=securepassword
      
      # Application settings
      # - ALLOW_REGISTRATION=false
